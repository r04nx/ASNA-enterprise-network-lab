apiVersion: 1

providers:
  - name: 'ASNA Dashboards'
    folder: 'ASNA'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
